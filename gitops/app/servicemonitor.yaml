apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: analytics
  namespace: analytics
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: analytics
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
