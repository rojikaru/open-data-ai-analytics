apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: analytics
  labels:
    app: analytics
spec:
  type: NodePort
  selector:
    app: analytics
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30000
